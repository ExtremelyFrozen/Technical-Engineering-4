# 机器 GUI 翻新 002 — 11 台未翻新机器 modular 素材族全覆盖

计划 ID：gui_renovation_002
状态：草稿
草稿路径：plans/.draft_plan_gui_renovation_002.md
正式路径：plans/plan_gui_renovation_002.md
下一步：计划审查

## 目标

将剩余 11 台未翻新机器 GUI（MatterCondenser/Beacon/MobRip/Quarry/Farm/Cell/CreativeCell/Solar/Engine×3/Pipe/Channel×3）从专属背景 PNG + EMPTY 槽 + HANDLER atlas gauge 翻新为统一空白底图 machine_gui.png + modular 素材族部件，布局遵循已翻新 7 台规则（能量条 x=8 锚定/组件群居中/中线 41/间距≥4），旧素材保留回退。

## 范围

- 包含：TENConstants.java（接线 5+2 新素材常量）
- 包含：TENMachineBlockUIFactory.java（progressGaugeWide/machineSlotPower/itemSlotModular/channel 面板 + backgroundFor 11 分支）
- 包含：11 台机器 createUI：Condenser/Beacon/MobRip/Quarry/Farm/Cell/CreativeCell/Solar/EngineBlockEntity/PipeBlockEntity/AbstractChannel×3
- 包含：验证：compileClientJava/compileTestJava/test/spotlessCheck/git diff --check
- 不包含：删除旧背景 PNG（11 个保留回退）
- 不包含：删除 handler.png atlas
- 不包含：玩家物品栏/升级槽/侧栏改造
- 不包含：机器名 Label 改造
- 不包含：JEI/XEI 兼容层
- 不包含：配方/注册项/语言键/模型
- 不包含：ModDev 游戏内视觉验证
- 不包含：Cable 非机器

## 执行顺序

1. P0 素材接线：TENConstants 新增 PROGRESS_BAR_WIDE_BG/FILL(80×5)、ITEM_SLOT_SMALL_POWER_I/O(18×18)、CHANNEL_LIST(69×72)；fuel_gauge_03 按 D5
2. P1 工厂扩展：progressGaugeWide(80×5 L2R)、machineSlotPower(chargeIn→power_i/o)、itemSlotModular(通用小槽，Pipe 用)、channel 面板方法（按 D4）；backgroundFor 11 分支→MACHINE_GUI
3. P2 Cell/CreativeCell：machineSlotPower 双槽（D2 语义）+ energyGaugeModular（D3 位置）
4. P3 Solar/Engine×3：energyGaugeModular+fuelGaugeModular（D5 素材），坐标保持
5. P4 Pipe：createRoot(MACHINE_GUI) + filterSlot 背景 modular（D6/D7）
6. P5 进度条 5 台（condenser/beacon/mob_ripper/quarry/farm）：progressGaugeWide（D1）+ machineSlotModular/fluidGaugeModular + 坐标设计（quarry/farm 进度 y=74 间距修正）
7. P6 Channel×3：按 D4 决策 A 整面板（channel_list 行高亮+滚动钮）或决策 B（保切片仅换背景）+ 槽/流体/能量切 modular
8. P7 验证收口：compileClientJava/test/spotless/git diff --check + grep 残留检查 + 艾琳审查门禁

## DoD

- [ ] P0：TENConstants 新增 PROGRESS_BAR_WIDE_BG/FILL、ITEM_SLOT_SMALL_POWER_I/O、CHANNEL_LIST 5 常量（fuel_gauge_03 按 D5），编译通过
- [ ] P1：工厂新增 progressGaugeWide/machineSlotPower/itemSlotModular/channel 面板方法；backgroundFor 11 台分支返回 MACHINE_GUI（Pipe 单独 createRoot）
- [ ] 11 台 createUI 无旧素材调用残留（grep 验证）
- [ ] 布局满足规则：能量条 x=8（Cell/Solar/Engine 按决策）、组件群水平居中、中线 41±2、间距≥4 无重叠
- [ ] 槽位背景全部 modular（ITEM_SLOT_SMALL/POWER_I/O/FLUID_SLOT）
- [ ] gauge 全部 modular（ENERGY_GAUGE/FUEL_GAUGE/PROGRESS_BAR_WIDE）
- [ ] Channel 滚动功能保持（ChannelUIState/scrollUp/scrollDown）
- [ ] Pipe 过滤逻辑零改动（isFiltered/白黑名单/mayPlace）
- [ ] 槽位/tank/进度/燃料/能量绑定不变
- [ ] compileClientJava/compileTestJava/cleanTest test 全量 0 failed
- [ ] spotlessCheck、git diff --check 通过
- [ ] 旧背景 PNG（11 个）+ handler.png 未删除（回退保障）
- [ ] 已翻新 7 台零改动（除 backgroundFor 新增分支）

## 风险与回退

- 风险：R1 channel_list 面板行高亮/滚动钮素材适配未知（高）→ P6 前素材检查，降级 D4 决策 B（保留 channel.png 切片）
- 风险：R2 Cell 能量条位置决策（中）→ 默认保持 (81,18) 居中
- 风险：R3 燃料素材选择（中）→ 默认 fuel_gauge_01（已接线零风险）
- 风险：R4 Quarry/Farm 进度条间距 3px（低）→ y=73→74
- 风险：R5 Condenser 输入槽左移 79→39（低）→ 回退保留 (79,32)
- 风险：R6 Pipe filterSlot 背景切换签名兼容（低）→ 复用 itemSlot 骨架
- 风险：R7 背景统一后视觉变化（低）→ 旧 PNG 保留回退
- 回退：执行前 git checkpoint
- 回退：P2~P6 每机器独立提交可单独 revert
- 回退：回退=恢复 checkpoint→还原 11 createUI+工厂+常量→背景改回旧 PNG
