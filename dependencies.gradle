dependencies {
    compileOnly(libs.jetbrains.annotations)

    testImplementation(libs.testframework.get())

    // LDLIB 2
    implementation(forge.ldlib2.get())
    jarJar(api(forge.ldlib2.get()))

    // Registrate — 无 26.1.2 版本，需迁移到 NeoForge DeferredRegister（Phase 1+）
    // jarJar(api(forge.registrate.get()))

    // Configuration — 无 26.1.2 坐标，需迁移到 ModConfigSpec（Phase 1+，不改源码）
    // jarJar(api(forge.configuration.get()))

    // Recipe Viewers — compileOnly unconditional
    compileOnly(forge.bundles.jei)

    // Standard runtime mods
    localRuntime(libs.testframework)

    // Development-only client runtime mods
    clientLocalRuntime(forge.jei.neoforge.impl)   // JEI runtime viewer
    clientLocalRuntime(forge.jade)                 // Jade debug overlay
    clientLocalRuntime(forge.modernfix)            // ModernFix: startup/loader optimizations
    clientLocalRuntime(forge.ferritecore)          // FerriteCore: memory reduction
    clientLocalRuntime(forge.modernui)             // ModernUI: UI library (Modrinth universal fat JAR)

    renderNurseCfg(libs.renderNurse)
}
