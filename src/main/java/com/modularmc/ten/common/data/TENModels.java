package com.modularmc.ten.common.data;

/**
 * Model/blockstate generation stubs — previously used by Registrate builder chains.
 * <p>
 * With the migration to DeferredRegister, model generation is deferred to a dedicated
 * {@link net.neoforged.neoforge.data.event.GatherDataEvent} provider.
 * The concrete model provider implementations will be recreated in a follow-up pass.
 * <p>
 * TODO(26.1.2): Rebuild as a proper ModelProvider for GatherDataEvent.
 */
public final class TENModels {

    private TENModels() {}
}
