feat(recipe): strict exact slot matching for machine recipes

FormsCombinedRecipe.matches(): occupied input slot count must equal
required item ingredient count before checking individual ingredients.

Previously any extra item in an unused input slot was silently ignored,
allowing recipes to match when unintended items were present.

feat(deps): add Powah! Rearchitected v6.2.8 as dev testing runtime mod